{% extends "base.twig" %}

{% block content %}

    {% include 'blocks/page-header.twig' %}

    {% if posts == [] %}
        {{ function('pll__', 'Sorry, no results were found.', 'theme') }}
    {% endif %}

    {% for post in posts %}
        {% include "templates/blocks/content.twig" with {post: post}  %}
    {% endfor %}

    {{ function ('posts_nav_link')}}

{% endblock %}
