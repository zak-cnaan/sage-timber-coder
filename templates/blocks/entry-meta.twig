<p class="byline author vcard lead">{{  __('By', 'sage') }} <a href="{{ post.author.path }}" rel="author" class="fn">{{ post.author.name }}</a></p>
<p><time class="updated" datetime="post.post_date|date(c)">{{ post.post_date|date }}</time></p>